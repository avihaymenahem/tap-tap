<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- The launch/splash theme. Android 12+ shows the system splash from these
         `windowSplashScreen*` attributes; the old `android:background=@drawable/splash`
         was silently ignored by that API. Backed by installSplashScreen() in
         MainActivity so pre-12 devices get the same treatment.

         The icon is a plain branded VECTOR (`@drawable/splash_icon`), NOT the
         adaptive `@mipmap/ic_launcher` — the splash API mis-renders adaptive
         icons on some OEMs (Samsung/One UI showed nothing/the stock robot),
         which is why the splash "didn't work". A vector on the navy background
         renders reliably and on-brand. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>